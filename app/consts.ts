import * as process from "process"

export const METADATA = {
    title: "Sineism",
    description: "Read utterances and comments from the Sineism community",
    base: process.env.NEXT_PUBLIC_SITE_URL || "http://localhost:3000",
    themeColor: "#FFF",
}
